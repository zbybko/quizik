OPENAI_API_KEY=sk-proj-your-key
OPENAI_MODEL=gpt-5.5
PORT=8787
APP_SHARED_SECRET=change-me

# Optional: external translation worker (POEditor-style). If unset, backend serves
# locale files from backend/locales/*.json on disk.
# LOCALE_WORKER_URL=https://locales.example.workers.dev
LOCALE_CACHE_TTL_MS=300000

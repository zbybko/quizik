{
  "name": "quizik-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit",
    "logs": "wrangler tail",
    "secret:openai": "wrangler secret put OPENAI_API_KEY",
    "secret:app": "wrangler secret put APP_SHARED_SECRET"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.92.0"
  }
}
